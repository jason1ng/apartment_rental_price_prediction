# Data (loaded via load_csv.py / kagglehub at runtime — never commit CSVs)
data/**/*.csv
data/**/*.xlsx

# Notebook checkpoints
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
.venv/
venv/
env/
rent_env/

# OS/editor junk
.DS_Store
.vscode/*
!.vscode/settings.json

# Streamlit secrets (never commit API keys/tokens)
.streamlit/secrets.toml

# Trained models ARE committed (see notebooks/03_modelling.ipynb) so the
# Streamlit app can load them directly without retraining on startup.
